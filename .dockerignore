# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual Environment
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Git
.git/
.gitignore
.github/

# Models are baked into the RunPod image (musetalk_runpod/Dockerfile).
# Do NOT exclude them here. The original api/main.py Dockerfile does not
# COPY models/, so leaving them in the build context only costs upload
# time for that variant — no harm done.
# models/

# Results (mounted as volume)
results/

# Temporary files
*.tmp
*.log
*.temp
*~
temp.mp4

# OS files
.DS_Store
Thumbs.db
.Spotlight-V100
.Trashes

# Cache
.cache/
.ruff_cache/
.pytest_cache/
.gradio/

# Training files (keep in repo, exclude from Docker)
train.py
train.sh
scripts/preprocess.py
scripts/realtime_inference.py

# Development/test files (keep in repo, exclude from Docker)
test_ffmpeg.py
test_api.py
inference.sh
entrypoint.sh
main.py
requirements.lock.txt

# Large/temporary media files
*.mp4
*.avi
*.mov
*.mkv
grok_*.mp4

# Documentation (not needed in runtime)
*.md
!README.md
LICENSE

# Lock files
uv.lock

# Docker files (not needed inside container)
Dockerfile
docker-compose*.yml
